import React, { useState, useEffect } from "react";

const { myAPI } = window;

export const App = () => {
  return (
    <div className="container">
      <h1>This is console!!</h1>
    </div>
  );
};
