import React, { useState, useEffect, useRef } from "react";

const { myAPI } = window;

import * as THREE from "three";

import { Town } from "./Town";

export const App = () => {
  return <Town />;
};
