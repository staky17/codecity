import React, { useState, useEffect, useRef } from "react";

const { myAPI } = window;

import * as THREE from "three";

import { City } from "./City";

export const App = () => {
  return <City />;
};
