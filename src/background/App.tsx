import React, { useState, useEffect } from "react";

const { myAPI } = window;

export const App = () => {
  return (
    <div className="container">
      <h1>This is background</h1>
    </div>
  );
};
